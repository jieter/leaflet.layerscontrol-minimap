{
  "commitMessage": "Bumped version to %s",
  "tagName": "%s",
  "scripts": {
      "preupdate": "npm run make && git commit L.Control.Layers.Minimap.js -m 'Browserified'",
      "postupdate": "git push && git push --tags && npm publish"
  }
}
